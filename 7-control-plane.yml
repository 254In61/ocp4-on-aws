---
# Build control plane or master nodes

- name: Build Control Plane nodes
  hosts: localhost
  gather_facts: false

  tasks:
    - name: include vars
      include_vars:
        dir: group_vars
    
    - import_role:
        name: control-plane