---
# Collect vpc cloudformation stack details
- name: network - Collect vpc cloudformation stack details
  set_fact:
    root_name: "{{ vpc }}"

- import_tasks: ../../../modules/obtain-stack-info.yml


# Create network and loadbalancers objects
- name: network - Set root name
  set_fact:
    root_name: "{{ ntwk_and_lbs }}"

- import_tasks: ../../../modules/infraname-and-hostedzone.yml

- import_tasks: ../../../modules/build-stack.yml