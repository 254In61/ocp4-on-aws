---
# Playbook for creating install-config.yaml >> manifests

- name: creating install-config.yaml
  hosts: localhost
  gather_facts: false

  tasks:
    - name: include vars
      include_vars:
        dir: group_vars

    - import_role:
        name: manifest-files